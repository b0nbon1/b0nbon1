---
title: Read, Write, Update and Delete files in Node.js (File Systems)
featured: True
date: 2021-10-12 00:00:00 +0300
description: Let's learn how to write files, read files, update files and delete files with Nodejs ~ Bonvic Bundi
tags: [Javascript, fs, Nodejs]
month: September
year: 2021
---

<br />

We use the `fs` module in Nodejs which allows you to work with the file systems on your computer.
You just Nodejs installed on your computer because `fs` module is part of the Nodejs core.

## Read Files

You can readfiles from your nodejs application using `fs.readFile` anf `fs.readFileSync`(for synchronous form) methods.

Examples for reading files:
We can read files a non-blocking asynchronously by `fs.readFile` or in synchronous way with `fs.readFileSync`.

#### Synchronous example

syntax for `readFileSync` is:

```
fs.readFileSync( path, options )
```

where **path** takes the relative path of the text file and **options** are the optional parameters like encoding and flag, you can refer more from [readFileSync options](https://nodejs.org/docs/latest-v15.x/api/fs.html#fs_fs_readfilesync_path_options)

```js
// reading textFile.txt
const fs = require('fs');

try {
  const fileFetched = fs.readFileSync('./textFile.txt', {
    encoding: 'utf8',
    flag: 'r',
  });

  // display data fetched
  console.log(fileFetched);
} catch (error) {
  console.log(error);
}
```

#### Asynchronous example

syntax for `readFile` is:

```
fs.readFile(path, options, callback)
```

More details about the parameters are available in [fs.readFile](https://nodejs.org/docs/latest-v15.x/api/fs.html#fs_fs_readfile_path_options_callback)

```js
const fs = require('fs');

readFile('./textFile.txt', (err, data) => {
  if (err) throw err;

  // display data fetched
  console.log(data);
});
```

