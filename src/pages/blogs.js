import * as React from "react"
import Layout from "../components/layout/Layout"

const BlogsPage = () => {
  return (
    <main>
      <Layout title="Blogs" />
    </main>
  )
}

export default BlogsPage
